function zerototen(){
    let num = Math.random();
    num *=10;
    num= Math.floor(num);
    console.log(' the first random number is ', num);
}
zerototen();
function zerotoonehundred(){
    let no = Math.random();
    no *=100;
    no= Math.floor(no);
    console.log(' the second number is ', no);
}
zerotoonehundred();
function zerototwo(){
   let numb = Math.random();
    numb *=2;
    numb=Math.ceil(numb)
    console.log(' the third number is ', numb);
}
zerototwo();